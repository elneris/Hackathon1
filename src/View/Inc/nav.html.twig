<div class="navbar navbar-inverse navbar-fixed-top headroom" >
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse"><span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span> </button>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav pull-left">
                <li {% if pageIndex %}class="active"{% endif %}><a href="/">Accueil</a></li>
                {% if session %}
                        {% if session.status == 0 %}
                            <li {% if pageMember %}class="active"{% endif %}><a href="/Member/index">Espace Membres</a></li>
                        {% endif %}
                        {% if session.status == 1 %}
                            <li {% if pageAdmin %}class="active"{% endif %}><a href="/Admin/index">Espace Admin</a></li>
                        {% endif %}

                {% endif %}
            </ul>
            <ul class="nav navbar-nav pull-right">
                {% if session %}
                    <li><a class="btn" href="/Login/logout">Se Deconnecter</a></li>
                {% else %}
                    <li><a class="btn" href="/Login/index">Se Connecter</a></li>
                {% endif %}
                <li><a class="btn" href="/register/index">S'inscrire</a></li>

            </ul>
        </div>
    </div>
</div>